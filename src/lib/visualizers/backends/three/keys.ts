import type { VisualizerPreset } from '../../types'

// Pure key list — no scene/three imports. Safe for unit tests + integrity checks.
export const THREE_SCENE_KEYS: readonly VisualizerPreset[] = [
  'neon-tunnel',
  'particle-galaxy',
]
