// Barrel for the shares feature. Re-exports the public surface so consumers
// can import from `~/lib/shares` rather than reaching into individual files.
//
// Modules are added incrementally as Phase 2 lands; this file is intentionally
// empty until the first helper exists, to avoid import-cycle risk during scaffold.

export {}
