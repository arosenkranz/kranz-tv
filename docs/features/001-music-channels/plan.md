# Implementation Plan: Music Channels

**Branch**: `001-music-channels` | **Date**: 2026-05-03 | **Spec**: [spec.md](./spec.md)
**Input**: Feature specification from `specs/001-music-channels/spec.md`

---

## Summary

Add music-only channels to KranzTV: SoundCloud playlists as audio sources, with procedural WebGL ambient backdrops replacing the video frame. The synchronized-viewing invariant (every viewer sees the same content at the same moment) is preserved by reusing the existing `getSchedulePosition()` scheduler, generalized to accept any media items with `id` and `durationSeconds`. SoundCloud track metadata is snapshotted at import time via the Widget API — eliminating runtime API dependencies. Backdrops are time-driven fragment shaders (not audio-reactive) implemented via a new `VisualizerRenderer` that shares a `ShaderQuadRenderer` base with the existing `OverlayRenderer`.

---

## Technical Context

**Language/Version**: TypeScript 5.x (strict mode)
**Primary Dependencies**: React 18, TanStack Start (routing + server fns), Vite, Vitest, Zod, SoundCloud Widget API (no package — postMessage)
**Storage**: localStorage (channel metadata), IndexedDB (track arrays — new)
**Testing**: Vitest + @testing-library/react; Playwright (E2E)
**Target Platform**: Browser (Cloudflare Workers for SSR shell); iOS Safari, Chrome, Firefox
**Project Type**: Web application (client-heavy; Workers for routing only)
**Performance Goals**: Backdrop GPU frame ≤4ms at 0.5x DPR mobile; page interactive ≤4s p95; sync accuracy p95 within 3s
**Constraints**: No Node-only APIs in Worker paths; ESM only; 80% test coverage; files ≤800 lines
**Scale/Scope**: Single-user personal KranzTV instance; up to ~20 imported channels at 50 tracks each

---

## Constitution Check

- [x] **I. Deterministic Scheduling** — `algorithm.ts` body is unchanged. Generalized to `Schedulable<TItem>` via a call-site adapter. Same pure-function invariant.
- [x] **II. Client-Side Data Fetching** — All SoundCloud Widget calls are browser-only. oEmbed (if used) is client-side. No new server functions proxy external requests.
- [x] **III. Test-First** — New pure functions (URL validator, `detectSource`, `dedupKey`, `toScheduleItem`, `toSchedulable`) get tests written before implementation. Task order enforces Red-Green-Refactor.
- [x] **IV. Observability** — New RUM actions: `music_channel_play`, `music_channel_import_success`, `music_channel_import_error`, `music_backdrop_selected`. DogStatsD: `kranz_tv.music.channel.play`, `kranz_tv.music.channel.import`. All emit non-PII fields per FR-019.
- [x] **V. Immutability & File Size** — All state transforms return new objects. New files projected ≤400 lines each. Source adapter split across 4 files; visualizer shaders each ~100 lines.
- [x] **Deployment** — No Node-only APIs. Version bump required in PR. No new top-level scripts; no new `tsconfig` entries needed for source files under `src/`.

---

## Project Structure

### Documentation (this feature)

```text
specs/001-music-channels/
├── plan.md              ← this file
├── spec.md
├── research.md
├── data-model.md
├── contracts/
│   ├── media-source-adapter.md
│   └── shader-quad-renderer.md
├── checklists/
│   └── requirements.md
└── tasks.md             ← generated by /speckit-tasks
```

### Source Code Changes

```text
src/
├── lib/
│   ├── scheduling/
│   │   ├── types.ts              MODIFY — discriminated union Channel; add Track, MusicChannel, Schedulable
│   │   ├── algorithm.ts          MODIFY — generalize to Schedulable; add toSchedulable()
│   │   └── epg-builder.ts        MODIFY — emit ScheduleItem instead of Video
│   ├── sources/                  CREATE — new directory
│   │   ├── types.ts              CREATE — MediaSource, MediaSourcePlayer, ImportedPlaylist, ImportError
│   │   ├── registry.ts           CREATE — detectSource(), sourceFor()
│   │   ├── index.ts              CREATE — barrel export
│   │   ├── soundcloud/
│   │   │   ├── adapter.ts        CREATE — SoundCloud MediaSource implementation
│   │   │   ├── widget.ts         CREATE — Widget postMessage wrapper (origin-validated)
│   │   │   ├── parser.ts         CREATE — isSoundCloudUrl(), exact-host allow-list
│   │   │   └── oembed.ts         CREATE — oEmbed fallback for display name
│   │   └── youtube/
│   │       └── adapter.ts        CREATE — YouTube MediaSource wrapping existing youtube-api.ts
│   ├── storage/
│   │   ├── local-channels.ts     MODIFY — strip tracks before persist; dedupKey(); re-validate URLs
│   │   └── track-db.ts           CREATE — IndexedDB saveTracks/loadTracks/deleteTracks
│   ├── overlays/
│   │   ├── renderer.ts           MODIFY — extract ShaderQuadRenderer base; OverlayRenderer extends it
│   │   └── shader-quad-renderer.ts CREATE — ShaderQuadRenderer base class
│   ├── visualizers/              CREATE — new directory
│   │   ├── types.ts              CREATE — VisualizerPreset enum ('spectrum'|'particles'|'kaleidoscope'|'oscilloscope')
│   │   ├── preset.ts             CREATE — URL param + localStorage default resolver
│   │   ├── renderer.ts           CREATE — VisualizerRenderer extends ShaderQuadRenderer
│   │   └── shaders/
│   │       ├── spectrum.glsl.ts  CREATE
│   │       ├── particles.glsl.ts CREATE
│   │       ├── kaleidoscope.glsl.ts CREATE
│   │       └── oscilloscope.glsl.ts CREATE
│   ├── import/
│   │   ├── import-channel.ts     MODIFY — delegate to detectSource(url)
│   │   └── schema.ts             MODIFY — discriminated union Zod schema + back-compat preprocess
│   └── datadog/
│       └── rum.ts                MODIFY — add SHA-256 helper; music channel RUM actions
├── components/
│   ├── music-channel-view.tsx    CREATE — SoundCloud Widget + visualizer canvas + Now Playing card
│   ├── music-visualizer-canvas.tsx CREATE — mounts VisualizerRenderer
│   ├── now-playing-card.tsx      CREATE — track title, artist, artwork, deep link
│   ├── channel-info-overlay.tsx  CREATE — extracted from _tv.channel.$channelId.tsx:638-700
│   ├── import-wizard/
│   │   └── import-tab.tsx        MODIFY — detectSource badge + source-aware submission
│   └── tv-player.tsx             MODIFY — AbortController mount discipline (retrofit)
├── hooks/
│   └── use-channel-navigation.ts MODIFY — 250ms debounce on navigation
└── routes/
    └── _tv.channel.$channelId.tsx MODIFY — kind === 'music' branch; extract ChannelInfoOverlay;
                                             feature-flag disabled placeholder

tests/
├── unit/
│   ├── lib/
│   │   ├── sources/soundcloud/
│   │   │   ├── parser.test.ts    CREATE
│   │   │   └── adapter.test.ts   CREATE
│   │   ├── sources/
│   │   │   └── registry.test.ts  CREATE
│   │   ├── storage/
│   │   │   ├── local-channels.test.ts MODIFY — back-compat + music channel tests
│   │   │   └── track-db.test.ts  CREATE
│   │   ├── import/
│   │   │   └── schema.test.ts    MODIFY — discriminated union tests
│   │   └── scheduling/
│   │       └── algorithm.test.ts MODIFY — MusicChannel fixture
│   └── components/
│       ├── music-channel-view.test.tsx CREATE
│       └── import-wizard/
│           └── import-modal.test.tsx   MODIFY
└── e2e/
    └── music-channel.spec.ts     CREATE — Playwright smoke test
```

---

## Implementation Phases

### Phase A — Type Foundation (prerequisite for all other phases)

**Goal**: Make the type system understand `VideoChannel | MusicChannel` without breaking existing code.

**Files**:

1. `src/lib/scheduling/types.ts` — Add `Track`, `MusicChannel`, `Schedulable`, `ScheduleItem`. Mark `Channel` as discriminated union. Keep all existing `Video` and `VideoChannel` (existing `Channel`) types.
2. `src/lib/import/schema.ts` — Discriminated union Zod schemas; `preprocess` for back-compat.
3. `src/lib/storage/local-channels.ts` — `dedupKey()` helper; strip tracks before persist; re-validate URLs on hydrate.

**Tests** (write first):

- `schema.test.ts`: union accepts both shapes; rejects mixed; preprocess injects `kind: 'video'` for legacy.
- `local-channels.test.ts`: back-compat round-trip; music channel metadata-only persist; URL re-validation.

**Completion signal**: TypeScript compiles with no new errors. All existing tests pass. New schema tests pass.

---

### Phase B — Source Adapter Layer

**Goal**: Abstract media-source detection and import behind the `MediaSource` interface. Move YouTube to an adapter. Add SoundCloud adapter.

**Files**:

1. `src/lib/sources/types.ts` — `MediaSource`, `MediaSourcePlayer`, `ImportedPlaylist`, `ImportError`.
2. `src/lib/sources/soundcloud/parser.ts` — `isSoundCloudUrl()` with exact-host allow-list.
3. `src/lib/sources/registry.ts` — `detectSource()`, `sourceFor()`.
4. `src/lib/sources/soundcloud/widget.ts` — Widget postMessage wrapper with `event.origin` validation.
5. `src/lib/sources/soundcloud/adapter.ts` — `importPlaylist()` via hidden iframe + polling; `createPlayer()` for playback Widget.
6. `src/lib/sources/youtube/adapter.ts` — Wrap existing `youtube-api.ts` behind `MediaSource` interface.
7. `src/lib/storage/track-db.ts` — IndexedDB `saveTracks/loadTracks/deleteTracks`.
8. `src/lib/import/import-channel.ts` — Delegate to `detectSource(url)` → `adapter.importPlaylist()`.

**Tests** (write first):

- `parser.test.ts`: valid SC URLs; negative cases for spoofed hosts, invalid protocols.
- `registry.test.ts`: `detectSource()` routes correctly; unknown URL returns null.
- `adapter.test.ts`: `importPlaylist()` with mocked Widget postMessage; origin validation rejects spoofed; polling fires until complete; >50 tracks rejects; timeout rejects.
- `track-db.test.ts`: IndexedDB round-trip; QuotaExceededError surfaces.

**Completion signal**: SoundCloud import works end-to-end in isolation (can be tested with a hidden iframe in the browser without any UI).

---

### Phase C — Scheduler Generalization

**Goal**: Make `getSchedulePosition()` work with `Track[]` as well as `Video[]`.

**Files**:

1. `src/lib/scheduling/algorithm.ts` — Add `toSchedulable(channel): Schedulable`; update `getSchedulePosition()` call sites to use it (or update signature to accept `Schedulable`).
2. `src/lib/scheduling/epg-builder.ts` — Introduce `ScheduleItem` view-model; add `toScheduleItem()` converter; emit `ScheduleItem` from `buildEpgRow()`.

**Tests** (write first):

- `algorithm.test.ts`: extend with `MusicChannel` fixture; assert same deterministic behavior at specific timestamps.

**Completion signal**: `getSchedulePosition()` returns the correct `Track` for a `MusicChannel` at specific timestamps. EPG builder produces `ScheduleItem` for both channel kinds.

---

### Phase D — Renderer Refactor

**Goal**: Extract `ShaderQuadRenderer` base class; implement `VisualizerRenderer` and four backdrop shaders.

**Files**:

1. `src/lib/overlays/shader-quad-renderer.ts` — Extract base class from `renderer.ts`.
2. `src/lib/overlays/renderer.ts` — `OverlayRenderer extends ShaderQuadRenderer`. Add `dispose()` with `loseContext()`. Public API unchanged.
3. `src/lib/visualizers/types.ts` — `VisualizerPreset` enum.
4. `src/lib/visualizers/preset.ts` — URL param + localStorage preset resolver.
5. `src/lib/visualizers/shaders/spectrum.glsl.ts` — Default preset. Faked EQ bars from sin/cos of `u_trackElapsed`.
6. `src/lib/visualizers/shaders/particles.glsl.ts` — Flow-field per-pixel noise with hue shift on `u_trackProgress`.
7. `src/lib/visualizers/shaders/kaleidoscope.glsl.ts` — Radial symmetry on slow noise.
8. `src/lib/visualizers/shaders/oscilloscope.glsl.ts` — Lissajous curves drifting with `u_trackElapsed`.
9. `src/lib/visualizers/renderer.ts` — `VisualizerRenderer extends ShaderQuadRenderer`. 60fps policy. `u_trackElapsed`, `u_trackProgress` uniforms. `prefers-reduced-motion` → still gradient. Auto-downgrade logic.
10. `src/components/music-visualizer-canvas.tsx` — React component mounting `VisualizerRenderer`. Accepts `preset`, `trackElapsed`, `trackProgress`.

**Tests** (write first):

- `renderer.test.ts` (visualizer): instantiates; accepts uniforms; `dispose()` calls `loseContext()`.

**Completion signal**: All four backdrop presets render without WebGL errors. `?viz=particles` URL param switches preset. `prefers-reduced-motion` replaces animation with still gradient.

---

### Phase E — Music Channel View

**Goal**: Wire SoundCloud playback, visualizer, Now Playing card, and resync logic into a `<MusicChannelView>` component.

**Files**:

1. `src/components/now-playing-card.tsx` — Track title, artist, artwork, progress indicator, optional "OPEN ON SOUNDCLOUD" link.
2. `src/components/music-channel-view.tsx` — Composes: SoundCloud Widget iframe (hidden, audio-only), `<MusicVisualizerCanvas>`, `<NowPlayingCard>`. Implements mount discipline (`AbortController`), seek-on-mount, drift-correction on first `PLAY_PROGRESS`, `FINISH` → advance, `ERROR` → skip slot.
3. `src/components/channel-info-overlay.tsx` — Extracted from `_tv.channel.$channelId.tsx:638-700`. Kind-aware deep link ("WATCH ON YOUTUBE" vs "OPEN ON SOUNDCLOUD").
4. `src/components/tv-player.tsx` — Retrofit: add `AbortController` per mount; abort check after each `await`.
5. `src/hooks/use-channel-navigation.ts` — Add 250ms debounce.
6. `src/routes/_tv.channel.$channelId.tsx` — Add `kind === 'music'` branch at ~line 608; extract `<ChannelInfoOverlay>`; add feature-flag disabled placeholder.

**Tests** (write first):

- `music-channel-view.test.tsx`: render with fixture `MusicChannel`; mock SC Widget postMessage; assert visualizer mounts; Now Playing card shows correct title/artist; rapid channel swap triggers abort + cleanup.

**Completion signal**: Navigating to a music channel renders the backdrop + Now Playing card + audio from SoundCloud Widget.

---

### Phase F — Import Wizard Integration

**Goal**: Auto-detect SoundCloud URLs in the existing import wizard. Show source badge. Route to correct adapter.

**Files**:

1. `src/components/import-wizard/import-tab.tsx` — On URL input change, call `detectSource(url)` → display badge ("SoundCloud Playlist" or "YouTube Playlist"). On submit, route to `soundcloudAdapter.importPlaylist()` or existing YouTube flow.

**Tests** (write first):

- `import-modal.test.tsx`: extend with SC URL path; badge appears for valid SC URL; badge absent for spoofed URL; >50-track playlist shows correct error.

**Completion signal**: Pasting a SoundCloud playlist URL into the existing import wizard creates a music channel.

---

### Phase G — EPG and Info Panel Integration

**Goal**: All EPG/info-panel components consume `ScheduleItem` view-model and render correctly for music channels.

**Files**:

1. `src/components/epg-overlay/epg-overlay-cell.tsx` — Render `primaryLabel` + optional `secondaryLabel`.
2. `src/components/tv-guide/guide-cell.tsx` — Same.
3. `src/components/info-panel/info-panel.tsx` — Use `ScheduleItem`; kind-aware deep link.
4. `src/components/mobile/mobile-now-playing.tsx` — Use `ScheduleItem`.

**Completion signal**: EPG shows "Track Title — Artist" for music channels and "Video Title" for video channels. Info panel deep link is "OPEN ON SOUNDCLOUD" for music channels.

---

### Phase H — Observability + Security Hardening

**Goal**: Add RUM events; add CSP baseline; finalize security requirements.

**Files**:

1. `src/lib/datadog/rum.ts` — Add `sha256Prefix(url)` helper; add `trackMusicChannelPlay`, `trackMusicChannelImport`, `trackMusicBackdropSelected`.
2. CSP baseline — Wire `Content-Security-Policy` header in the Cloudflare Worker response (in the appropriate server entry point or `wrangler.toml` headers config). See contract for header value.

**Completion signal**: Datadog RUM receives music channel events in dev. No raw URLs or track titles in RUM payloads.

---

### Phase I — Version Bump + PR

**Goal**: Bump version, run lint + typecheck + tests, create PR.

1. Bump version in `package.json` (minor: `1.x.0` → `1.(x+1).0`).
2. `pnpm lint && pnpm test -- --run` — must pass with ≥80% coverage.
3. `gh pr create` with HEREDOC body summarizing the feature.

---

## Complexity Tracking

No constitution violations. No unjustified complexity.

| Decision                       | Why                                                                                 | Alternative rejected                                                  |
| ------------------------------ | ----------------------------------------------------------------------------------- | --------------------------------------------------------------------- |
| IndexedDB for track arrays     | localStorage quota ceiling at scale; trust boundary                                 | All-localStorage: Safari eviction risk + O(n) stringify on every save |
| ShaderQuadRenderer base class  | OverlayRenderer has 30fps + fixed uniforms; visualizer needs 60fps + extra uniforms | Retrofit OverlayRenderer: blends two responsibilities in one class    |
| Discriminated union            | VideoChannel and MusicChannel have genuinely divergent shapes                       | Flat optional fields: allows invalid states, `?.` everywhere          |
| Widget-at-import-time snapshot | Only path to per-track durations without an API key                                 | Nothing viable: SC HTTP API closed to new registrations               |
| 50-track ceiling               | getSounds() lazy-hydrates; above this, snapshot is unreliable                       | No ceiling: would silently truncate large playlists                   |
